.input.form
  = form_with model: @msg do |form|
    = form.label 'Mensagem'
    %fieldset{role: 'group'}
      = form.text_field :text
      = form.hidden_field :user_id, value: current_user.id
      = form.submit 'Enviar'
